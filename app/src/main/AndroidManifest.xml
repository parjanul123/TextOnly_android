<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 🔹 Permisiuni necesare -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- ✅ Pentru a citi și scrie în Firestore fără probleme -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- 🔹 Suport pentru toate ecranele -->
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:resizeableActivity="true"
        android:theme="@style/Theme.TextOnly"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- 🟢 Login -->
        <activity
            android:name=".Login"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 💬 Lista conversațiilor -->
        <activity
            android:name=".ChatActivity"
            android:exported="false"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout" />

        <!-- 💸 Pagina OnlyCoins -->
        <activity
            android:name=".BuyOnlycoinsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 📷 Scanare coduri QR -->
        <activity
            android:name=".ScanQRActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|smallestScreenSize"
            android:resizeableActivity="true" />

        <!-- 💬 Fereastra individuală de chat -->
        <activity
            android:name=".ChatWindowActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />

        <!-- 🎥 Activitate apeluri WebRTC -->
        <activity
            android:name=".CallActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:hardwareAccelerated="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:usesCleartextTraffic="true" />

        <!-- 🧩 Activitate pentru chat WebRTC (text p2p) -->
        <activity
            android:name=".WebRTCChatActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:hardwareAccelerated="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:usesCleartextTraffic="true" />

    </application>

</manifest>
